<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="MeetupApp.Templates.SocialIcon">
    <ContentView.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding Command}" CommandParameter="{Binding CommandParameter}"/>
            </Grid.GestureRecognizers>
            <Image Source="{Binding ImageSource}"/>
            <Label Grid.Row="1" Text="{Binding LabelText}"
                HorizontalTextAlignment="Center" 
                HorizontalOptions="CenterAndExpand"/>
        </Grid>
    </ContentView.Content>
</ContentView>
